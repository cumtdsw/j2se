package com.pugwoo;

/**
 * 关于渲染字体：要注意部署到服务器上字体的渲染问题，所以
 * 一般把自己打包到war包中，然后指定好字体，这样程序部署到任何操作系统，都是没有问题的
 * @author nick
 */
public class RenderText {

	public static void main(String[] args) {
		/**
		            Font font = Font.createFont(Font.TRUETYPE_FONT,TestImageIo.class.getResourceAsStream("/msyh.ttc"));
            Font deriveFont = font.deriveFont(25f);
            graphics2D.setFont(deriveFont);
            graphics2D.setColor(Color.BLACK);
            graphics2D.drawString(nickName, 180, 360);
		 */
	}
}
