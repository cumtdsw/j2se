2015年6月5日 13:49:05

def add(a, b) {
    a+b; // 约定最后一条表达式的返回值作为整个方法的返回值
    // 当然写return a+b;也行，我更喜欢有return的写法，语义清晰。
}

【特别注意】定义方法只能执行一次，不能重复定义方法。

和javascript还挺像的，可以定义匿名函数，这个就可以重复执行，例如：
add2 = def (a,b){a+b};
add2(1,2)

所以，在mvel中，函数也是一等公民，可以作为参数传入另一个函数。
